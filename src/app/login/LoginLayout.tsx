import React, { ReactNode } from 'react';
import { Montserrat } from 'next/font/google';
import '../globals.css';

const LoginLayout = () => {
  return (
    <div className='h-screen flex justify-center items-center bg-black bg-opacity-60'></div>
  );
};

export default LoginLayout;
